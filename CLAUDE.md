# CLAUDE.md

このリポジトリで作業する際の注意事項。

## 個人情報を書かない

このリポジトリはオープン（公開）にしている。以下をコミットする内容に含めないこと。

- ユーザーの個人名（例：本名、ニックネーム）
- ユーザー個人の考え・状況の説明（例：「開発者本人（〇〇）は〜」といった個人の事情の記述）
- メールアドレスなどの個人を特定できる情報

企画・設計ドキュメントでユーザー自身に言及する必要がある場合は、「開発者本人」のように匿名・抽象化した表現を使うこと。

## 有用だが公開できない情報の扱い

上記に該当する情報でも、作業を進める上で有用な場合は削除せず `private/` ディレクトリに書き残す。`private/`は`.gitignore`で除外済みのため、パブリックリポジトリには一切上がらない。詳細は`private/README.md`を参照。

## 作成とレビューの分離

公開対象の文書（`private/`以外の全ファイル）を新規作成・大きく変更した際は、作成した本人とは別の視点で、公開して問題ないかをレビューする（可能であれば別エージェントに担当させる）。レビューでは以下を確認する。

- 個人名・個人の状況・メールアドレス等が含まれていないか
- 本プロダクトと無関係な情報（他プロダクト名・社内事情等）が含まれていないか
- バイナリファイル（docx等）のメタデータ（作成者情報・コメント等）に非公開情報が残っていないか

問題が見つかった場合、作業を進める上で有用なら`private/`へ退避し、無関係・不要なら削除する（退避するか削除するかは文脈で判断し、迷う場合はユーザーに確認する）。
